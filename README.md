# Advent of Code 2025 (Rust)

Each solution is implemented in a separate binary.

Pass the week number to cargo to run.
Most binaries require an input text file.

E.g. for week one
```bash
cargo run --bin 1 testdata.txt
```

